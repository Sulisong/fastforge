name: flutter_distributor_workspace
publish_to: "none"

environment:
  sdk: ">=2.16.0 <4.0.0"

scripts:
  构建amd64: dart compile exe packages/flutter_distributor/bin/main.dart -o main_amd64
  构建arm64: dart compile exe packages/flutter_distributor/bin/main.dart -o main_arm64

dev_dependencies:
  flutter_lints: ^2.0.0
  melos: ^3.1.0
